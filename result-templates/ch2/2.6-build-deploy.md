# 2.6 Notiflex 앱 생성·빌드·배포 — 예상 결과

## 상태 확인

```
$ kubectl get pods -n notiflex
NAME                            READY   STATUS    RESTARTS   AGE
notiflex-api-xxxxxxxxxx-xxxxx   1/1     Running   0          30s
notiflex-api-xxxxxxxxxx-xxxxx   1/1     Running   0          30s

$ curl http://localhost:8080/health
{"status":"ok"}

$ curl http://localhost:8080/id
{"id":"1","generated_by":"notiflex-api-xxxxxxxxxx-xxxxx"}
```

## 체크리스트

- [ ] Artifact Registry에 notiflex/api:v0.1.0 이미지 푸시됨
- [ ] notiflex 네임스페이스 생성됨
- [ ] Pod 2개 Running
- [ ] /health → 200 OK
- [ ] /id → 순차 ID 생성
- [ ] JOURNEY.md `현재 버전` — Notiflex 이미지 `v0.1.0`, Go `1.25` 기재

## 이 단계 이후 아키텍처

```
GKE: notiflex-cluster
├── default-pool: e2-medium x2
└── notiflex namespace
    ├── Deployment: notiflex-api (replicas: 2, v0.1.0)
    └── Service: notiflex-api (ClusterIP, 80→8080)
```

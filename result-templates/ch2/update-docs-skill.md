# /update-docs 커스텀 스킬 — 예상 결과

## 상태 확인

```
$ ls .claude/commands/
update-docs.md

$ wc -l .claude/commands/update-docs.md
      30 .claude/commands/update-docs.md
```

## 체크리스트

- [ ] `.claude/commands/update-docs.md` 생성
- [ ] frontmatter `description` 포함
- [ ] 실행 절차 3단계 명시
- [ ] 후보 5개(JOURNEY.md, CLAUDE.md, ADR, claude-context/, command-guardrails/) 명시
- [ ] "존재하는 것만 처리", 각 항목 도입 시점 명시

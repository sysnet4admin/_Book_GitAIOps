# 6.3 Canary 배포 — 예상 결과

## 상태 확인

```
$ kubectl argo rollouts get rollout notiflex-api -n notiflex
Name:            notiflex-api
Namespace:       notiflex
Status:          ✔ Healthy
Strategy:        Canary
  Step:          6/6
  SetWeight:     100
  ActualWeight:  100
Images:          asia-northeast3-docker.pkg.dev/.../api:v0.3.0 (stable)
```

## 체크리스트

- [ ] Blue/Green → Canary 전략 전환 완료
- [ ] 기존 Rollout 삭제 후 재생성 확인
- [ ] 20% → 50% → 80% → 100% 점진 배포 동작 확인
- [ ] 각 단계에서 30초 pause 후 자동 진행
- [ ] JOURNEY.md `도구 선택 기록` — 배포 전략 전환 이유 추가 (예: `| 배포 전략 전환 (ch6.3) | Argo Rollouts Canary | Blue/Green | ...`)
- [ ] /update-docs 실행 안내됨

## 이 단계 이후 아키텍처

```
GKE: notiflex-cluster
├── notiflex namespace
│   ├── Rollout: notiflex-api (Canary, v0.3.0)
│   │   └── steps: 20% → 50% → 80% → 100%
│   ├── Service: notiflex-api (stable)
│   ├── Service: notiflex-api-preview (canary)
│   ├── Valkey + Secret Manager
│   └── Gateway + HTTPRoute
├── monitoring namespace
├── argo-rollouts namespace
└── argocd namespace

배포 전략: Rolling → Blue/Green → Canary (진화)
```

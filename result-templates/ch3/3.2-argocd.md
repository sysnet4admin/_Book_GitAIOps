# 3.2 ArgoCD 설치 및 GitOps 연결 — 예상 결과

## 상태 확인

```
$ kubectl get pods -n argocd
NAME                                  READY   STATUS    RESTARTS   AGE
argocd-server-xxxxxxxxxx-xxxxx        1/1     Running   0          2m
argocd-repo-server-xxxxxxxxxx-xxxxx   1/1     Running   0          2m
argocd-application-controller-0       1/1     Running   0          2m
...

$ kubectl get application -n argocd
NAME            SYNC STATUS   HEALTH STATUS
notiflex-smb    Synced        Healthy
```

## 체크리스트

- [ ] ArgoCD 모든 Pod Running
- [ ] ArgoCD UI 접속 가능 (https://localhost:8443)
- [ ] notiflex-smb Application 생성됨
- [ ] Sync Status: Synced, Health: Healthy
- [ ] auto-sync, prune, selfHeal 활성화됨
- [ ] (private 저장소인 경우) GitHub 토큰 설정됨
- [ ] JOURNEY.md `현재 버전` — ArgoCD 버전 기재 (`kubectl get deploy argocd-server -n argocd -o jsonpath='{.spec.template.spec.containers[0].image}'`로 조회)

## 이 단계 이후 아키텍처

```
GitHub: notiflex-platform (k8s/smb/ 감시)
         ↓ auto-sync
GKE: notiflex-cluster
├── argocd namespace
│   └── ArgoCD (Server, Repo Server, Controller)
└── notiflex namespace
    ├── Deployment: notiflex-api (v0.1.0)
    └── Service: notiflex-api
```
